#' Tidy spatial data
#' `geotidy` uses tibbles to manipulate simple featues. It uses the [sf] package under the hood. There are a few minor differences, but if you're already using a tidy workflow with `sf`, most of the code is compatible already. `geotidy` uses a subset of `sf`.
#' @keywords internal
#' @importFrom purrr map map2
#' @importFrom sf st_point NA_crs_
#' @import tibble dplyr
"_PACKAGE"

## geptidy namespace: start
## geotidy namespace: end
NULL
